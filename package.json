{
  "name": "doorhandler",
  "private": true,
  "version": "1.0.0",
  "description": "Door RFID Handler that looks for an uid, notifies the user and opens the door on confirmation",
  "engines": {
    "node": ">= 6.9.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node src/index.js"
  },
  "author": "robbi5 <robbi5@robbi5.de>",
  "dependencies": {
    "axios": "^0.21.1",
    "body-parser": "^1.19.0",
    "dotenv": "^4.0.0",
    "express": "^4.17.3",
    "express-rate-limit": "^5.1.3",
    "querystring": "^0.2.0",
    "yaml": "^1.10.0"
  }
}
